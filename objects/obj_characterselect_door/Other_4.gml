with (obj_player)
{
    if (character == "P")
    {
        if ispeppino
            other.char = 0
        else
            other.char = 1
    }
    if (character == "N")
        other.char = 1
    if (character == "V")
        other.char = 2
    if (character == "S")
        other.char = 3
}
